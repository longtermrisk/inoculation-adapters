# Pod dependency spec — compiled to pod-requirements.txt with:
#   uv pip compile pod-requirements.in -o pod-requirements.txt --python-version 3.11
# torch is pinned to the version baked into the bellhop `pytorch-cuda` image
# preset (runpod/pytorch:2.4.0-py3.11-cuda12.4.1) so resolution can't drift it.
torch==2.4.1
transformers>=4.44,<5
peft>=0.11
accelerate>=0.30
datasets>=2.19
langdetect>=1.0.9
safetensors
